// Vamos criar variaveis usando let, const e var, atribuir strings e vamos concatenar e interpolar.

var nome = "Rodrigo";
let sobrenome = "Alexandre";
const empresa = "VTEX";

console.log("Nome:", nome + ", Sobrenome:", sobrenome + ", Empresa:", empresa);
console.log(`Nome: ${nome}, Sobrenome: ${sobrenome}, Empresa: ${empresa}`);